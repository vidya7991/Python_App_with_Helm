
---
apiVersion: v1
kind: Service
metadata:
  # namespace: py-demo-1
  name: service-2048
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: app-2048